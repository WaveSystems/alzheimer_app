!!!
%html
  %head
    %title Alzheimer
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %header.topnav
      %nav.navbar.navbar-fixed-top
        %div.navbar-inner
          %div.container
            .brand Reminiscense
            = render 'layouts/top-navbar'
    %div
      - if notice
        %div.alert.alert-success
          = notice
      - elsif alert
        %div.alert
          %div= alert
    %article
      #render_views
        = yield
    - if user_signed_in?
      #notify
        #title_bar
          #notify-icon
    - else
      %input{:type => 'hidden', :value => 'Sesion no iniciada', :class => 'log_in'}
    %footer(align = "center")
      %h3 &copy; WaveSystems 2012
